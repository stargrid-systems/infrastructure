variant: flatcar
version: 1.1.0

storage:
  filesystems:
  - device: ${yamlencode(volume_linux_device)}
    format: ext4
    # Only wipes if the filesystem doesn't match.
    wipe_filesystem: true
    label: ephemeral1
